# Deploy Status

Not applicable — this is an iOS app (bundle ai.palmr.emma). Deployment is via Xcode Archive + TestFlight, not a web deployment target.

Build verified on iOS Simulator (iPhone 17 Pro, iOS 26.4).
